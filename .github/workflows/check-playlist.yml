name: Check playlist

on:
  schedule:
    - cron: "20 4 */6 * *"   # every 6 days at 04:20 UTC
  workflow_dispatch:          # also runnable by hand from the Actions tab

permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install -r requirements.txt

      - name: Test every stream
        run: python3 check_streams.py list.m3u

      - name: Commit the cleaned playlist
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add list.m3u removed.m3u strikes.json health.md
          if git diff --staged --quiet; then
            echo "no changes"
          else
            git commit -m "Remove dead channels ($(date -u +%Y-%m-%d))"
            git push
          fi
